{% extends 'IntegratedSubscriptionBundle::base.html.twig' %}

{% block body %}
    <div id="wrapper-holder">
        <div class="container">
            <div class="content_columns_holder">
                <div class="content_column_inner">
                    {{ form(form) }}

                    <a href="{{ path('integrated_subscription_show_wall') }}">Show walls</a>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}